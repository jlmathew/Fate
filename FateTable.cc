#include "FateTable.h"
/*
class TableRouteInfo : public TableBase
{
	 
	public:
    TableRouteInfo();
    virtual ~TableRouteInfo();k
  
    GetAllPhys(std::list< PhyName &phyName>);
    GetAllDestinations(const PhyName &phyName, std::list< NodeName> &destination);
    GetAllNeighbors(const PhyName &phyName, const NodeName &destination, NodeName &neighbor);
    GetAllProperties(const PhyName &phyName, const NodeName &destination,const NodeName &neighbor,  const RouteInfo &updatedInfo);
    UpdateTable(const PhyName &phyName, const NodeName &destination,const NodeName &neighbor,  const RouteInfo &updatedInfo);
    UpdateTable(const PhyName &phyName, const NodeName &destination,const NodeName &neighbor,  const RouteInfo &updatedInfo, std::list<FullRouteCriteria> &uniqueRouteCharacteristics);
    //GetEntries(const PhyName &phyName, const NodeName &neighbor, const NodeName &destination, std::list <RouteInfo> &updatedInfo);
    GetMatches(const std::list<PhyName> &phyList, const std::list<NodeName> &destList, const std::list<NodeName> neighborList, const std::list< RouteCriteria> &routeCriteriaList, std::list<FullRouteInfo_t> &routeMatchList);


};
*/
